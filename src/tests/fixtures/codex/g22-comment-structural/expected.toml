# TODO: fix the [broken thing
[mcp_servers.other]
command = "node"

# a stray quote run """ and an apostrophe run ''' in prose
[mcp_servers.mcp]
command = "npx"
args = ["-y", "@yawlabs/mcp@latest"]
startup_timeout_sec = 60.0
